# Market data
yfinance>=0.2.40
pandas>=2.2.0
numpy>=1.26.0

# Config
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# Persistence (Postgres in prod, SQLite for dev/tests via the same code path)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# Logging
loguru>=0.7.0

# Tests
pytest>=8.0.0
